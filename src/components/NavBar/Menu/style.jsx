import styled from "styled-components";

export const Wrapper = styled.nav`
    color: var(--golden-pink-light);
    display: flex;
    flex-direction: column;
    padding: 2rem;
`;
export const ContainerNavItem = styled.li`
    align-items: end;
    display: flex;
    font-size: 2rem;
    gap: 2rem;
    margin: 1rem 0;
`;